---
# tasks file for role_vmrun
- name: "({{ ROLE_ID }} - 000.01) To execute vmrun command"
  shell: >
      "/Applications/VMware Fusion.app/Contents/Public/vmrun" -T {{ vmrun_type }} {{ vmrun_state }} "{{ vmx_directory }}{{ item }}" {{ vmrun_parameter }}
  args:
      executable: /bin/bash
  loop: "{{ vmx_file_list }}"
